name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      post_id:
        description: '投稿ID'
        required: true
        type: string

permissions:
  contents: read
  actions: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    - run: npm ci
    - run: npm test
    - run: npm run lint

  news-processing:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    - run: npm ci
    - name: Fetch News (Import + Process)
      env:
        WP_URL: ${{ secrets.WP_URL }}
        WP_AUTH: ${{ secrets.WP_AUTH }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: npm run fetch-news
