name: CI

run-name: |
  ${{ github.event_name == 'schedule' && github.event.schedule == '*/5 * * * *' && '📥 5分おきニュースインポート' ||
     github.event_name == 'workflow_dispatch' && format('🔧 手動実行: 投稿ID {0}', github.event.inputs.post_id) ||
     github.event_name == 'pull_request' && format('🔍 PRテスト: {0} → {1}', github.head_ref, github.base_ref) ||
     github.event_name == 'push' && format('🚀 プッシュテスト: {0}', github.ref_name) ||
     '🏗️ CI実行' }}

on:
  # プッシュ時とプルリクエスト時に実行
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  # 手動実行
  workflow_dispatch:
    inputs:
      post_id:
        description: '投稿ID'
        required: true
        type: string
  # 5分おきに実行
  schedule:
    - cron: '*/10 * * * *'   # 5分おき

permissions:
  contents: read
  actions: read

jobs:
  # --- プッシュやプルリク時にのみ実行するジョブ ---
  test:
    # 実行条件: イベントが 'schedule'（タイマー）ではない時
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    - run: npm ci
    - run: npm test
    - run: npm run lint

  # --- 5分おきにニュースインポートを実行するジョブ ---
  importing:
    # 実行条件: 5分おきのスケジュール実行時
    if: github.event_name == 'schedule' 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    - run: npm ci
    - name: Import News Only
      env:
        WP_URL: ${{ secrets.WP_URL }}
        WP_AUTH: ${{ secrets.WP_AUTH }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: npm run fetch-news